---
import BaseLayout from '../layouts/BaseLayout.astro';
import { ChevronLeft } from 'lucide-astro';
---

<BaseLayout title="Página no encontrada">
  <div class="prose dark:prose-invert max-w-none">
    <div class="min-h-[50vh] flex items-center">
      <div class="space-y-4">
        <div class="flex items-baseline gap-4">
          <span class="text-3xl font-semibold text-gray-400 dark:text-gray-500">404</span>
          <h1 class="text-2xl font-semibold mt-0 mb-0">Página no encontrada</h1>
        </div>
        
        <p class="text-gray-600 dark:text-gray-300 m-0">
          Lo sentimos, no pudimos encontrar la página que estás buscando.
        </p>

        <a
          href="/"
          class="inline-flex items-center gap-1 text-blue-600 hover:text-blue-500 dark:text-blue-400 dark:hover:text-blue-300 no-underline hover:underline"
        >
          <ChevronLeft class="w-4 h-4" />
          Volver al inicio
        </a>
      </div>
    </div>
  </div>
</BaseLayout>
